# Python bytecode cache (generated by waybar/scripts/volume-slider.py, etc.)
__pycache__/
*.pyc

# Live theme selections (phase 7): local runtime state, like the wallpaper.
# The committed defaults (red-black palette, frozen swaybg line) are the
# fallback when these are absent, so a fresh clone works with no state.
theme/state/
